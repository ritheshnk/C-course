#include<stdio.h>
int main()
{
  int a=90;
  int* ptra=&a;
  printf("the value of a without using pointer is %d\n",a);
  printf("the value of a using pointer is %d\n",*ptra);
   printf("the address  of ptra  is %x",*ptra);
}
